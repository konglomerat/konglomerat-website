prototype(Konglomerat.Website:LogowallLogo) {
    url = Neos.Neos:ImageUri {
        asset = ${q(node).property('image')}
        maximumHeight = ${q(node).property('height') * 2}
        allowCropping = FALSE
        allowUpScaling = TRUE
    }
}